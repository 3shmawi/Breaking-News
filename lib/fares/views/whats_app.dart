import 'package:flutter/material.dart';

class FaresWhatsapp extends StatelessWidget {
  const FaresWhatsapp({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.green,
        foregroundColor: Colors.white,
        centerTitle: false,
        title: Text("Fero & Sonson & Deda & Eso & Yaso & Azza Whatsapp"),
      ),
    );//alt enter
  }
}
