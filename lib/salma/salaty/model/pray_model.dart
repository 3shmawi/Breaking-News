class PrayModel {
  String title;
  String time;
  String icon;
  bool isMuted;
  bool isSelected;

  PrayModel({
    required this.title,
    required this.time,
    required this.icon,
    required this.isMuted,
    required this.isSelected,
  });
}
